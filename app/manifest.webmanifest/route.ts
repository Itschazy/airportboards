import { redirect } from 'next/navigation';

/**
 * Запасной адрес манифеста без локали.
 *
 * Был `app/manifest.ts` — файловая конвенция Next, которая сама вставляет
 * `<link rel="manifest" href="/manifest.webmanifest">` в <head> И ПЕРЕБИВАЕТ поле
 * `manifest` из generateMetadata. Пока файл существовал, ссылка на локальный манифест
 * в разметку не попадала вовсе, сколько бы её ни объявляли в метаданных.
 *
 * Адрес сохранён, потому что на него ссылаются уже установленные приложения и его
 * запрашивают краулеры; он ведёт на английский манифест явно, а не через редирект
 * корня по Accept-Language, который открывал бы приложение в непредсказуемом языке.
 */
export function GET() {
  redirect('/en/manifest.webmanifest');
}
